GitHub Actions OIDC assumes the DeployRole. DeployRole can iam:PassRole its way to the
RuntimeRole. RuntimeRole reads the customer-exports bucket, so the customer export data is
what the CI identity reaches.
